#pragma once

namespace game {

enum class SoundId {
    PlayerMove,
    PlayerCollision,
    EnemyCollision,
    // Add more sound IDs as needed
};

enum class MusicId {
    MainTheme,
    BattleTheme,
    // Add more music IDs as needed
};

} // namespace game
